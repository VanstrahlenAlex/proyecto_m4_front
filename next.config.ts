import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
	images: {
		domains: ['www.apple.com'], // Agrega aquí el dominio de donde provienen las imágenes
	},
};

export default nextConfig;
